# PES Arena runtime logs

Local development can write rotating JSON-lines logs to `logs/pes_arena.log`.
Runtime `.log` files are ignored by Git. Production/serverless logs are written to stdout for Vercel capture.
